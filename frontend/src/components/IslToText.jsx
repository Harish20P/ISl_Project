import React from "react";

const ISLToText = () => {
  return (
    <div>
      <h1>ISL to Text</h1>
      <p>This is the page where ISL to text conversion happens.</p>
    </div>
  );
};

export default ISLToText;